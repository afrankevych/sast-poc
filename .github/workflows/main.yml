on: [push]

jobs:
  test_job:
    runs-on: ubuntu-latest
    name: Test job
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v3
      - name: scans dependencies for vulnerabilities
        run: sh bin/scan.sh
#      - name: Hello world action step
#        uses: ./.github/actions/test # Uses an action in the root directory
